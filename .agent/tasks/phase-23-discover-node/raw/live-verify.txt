Phase 23 - DiscoveryService DiscoverNode (node-scoped device discovery, read-only)
Live evidence against the demo stand (host <demo-host>, creds <redacted>).
Exercised end-to-end THROUGH the shipped MCP tool (axxon_mcp_discovery
.discover_node_devices). DiscoverNode is the node-scoped twin of the shipped
Discover; results stream from GetNodeDiscoveryProgress. Read-only network scan,
no VMS mutation, no approval gate (mirrors discover_devices).

[node identifier] (probe findings)
  node="Server" (TLS CN)   -> DiscoverNode OK; progress yields pages
  node="hosts/Server"      -> RpcError "Can't get connection channel!" (wrong form)
  node="" (empty)          -> DiscoverNode OK (current node assumed)

[discover_node_devices] node="Server", max_devices=50, max_seconds=12
  status=ok discover_started=True
  state=PROGRESS_STATE_RUNNING promille=726
  count=3 devices found
  progress_timed_out=True   (scan still running at the bound; handled gracefully,
                             partial devices returned instead of raising)
  device shape: driver/driver_version/vendor/model/mac_address/ip_address/
                ip_port/firmware_version/categories/support_mode (MAC/IP -> <device>)

[notes]
  GetNodeDiscoveryProgress is intermittently slow on this single-node stand and can
  hit the per-call timeout before yielding; the tool catches DEADLINE_EXCEEDED and
  reports progress_timed_out=True with whatever it gathered. DiscoverNode itself
  returned cleanly on every run (3/3 standalone).

[hygiene]
  Read-only: nothing on the stand was created, changed, or deleted. No proto/CA/PDF
  committed. Secrets env-only. No biometric data.
