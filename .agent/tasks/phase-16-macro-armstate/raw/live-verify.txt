Phase 16 — LogicService control tools (launch_macro, change_arm_state)
Live evidence against the demo stand (host/creds redacted; AXXON_TLS_CN=Server).
Exercised through the MCP tools with AXXON_LOGIC_CONTROL_APPROVE=1.

[list_launchable_macros] ListMacros
  total=24 macros; 9 classified launchable (manual, mode.common), 15 autorule.
  launchable sample: Fire, Macro1, Macro2 (guids are object ids).

[launch_macro] LaunchMacro
  GATE  bad confirmation token            -> status=gap (no wire call)
  LAUNCH manual macro "Fire"              -> status=launched

[change_arm_state] ChangeArmState (bounded, auto-reverting)
  camera_ap = hosts/Server/DeviceIpint.1/SourceEndpoint.video:0:0
  arm    (timeout 5s)                     -> status=applied auto_reverts=true
  disarm (timeout 5s)                     -> status=applied auto_reverts=true
  bad state "explode"                     -> status=error (no wire call)
  missing/zero timeout                    -> status=error (no permanent change allowed)
  state map: disarm->CS_Disarm, arm->CS_Arm, arm_private->CS_ArmPrivate.

[hygiene]
  Every arm-state change carried a 5s timeout; the server reverts to the prior
  state after the window. Confirmed cam1 reachable after the timeouts elapsed; no
  residual armed state, no alerts, no residue. No biometric data involved.

NOT in scope (stay pending): ChangeMacros, ChangeConfig, ChangeCounters,
CounterAction (LogicService); macro CRUD is handled by the operator module.
