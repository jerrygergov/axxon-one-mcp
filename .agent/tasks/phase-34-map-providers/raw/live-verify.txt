Phase 34 - MapService.ConfigureMapProviders + GetMapProvider (completes MapService)
Live evidence against the demo stand (host <demo-host>, creds <redacted>).
Exercised end-to-end THROUGH the shipped MCP tools (axxon_mcp_map_providers.
configure_map_providers / get_map_provider). configure is approval-gated.

Verification is reversible: a throwaway provider is created with a random GUID, read
back, then removed; nothing real is touched (the 2 stock providers are untouched).

[gate matrix]
  env unset                  -> configure_map_providers status=disabled (no wire call)
  enabled + bad token        -> status=gap   (no wire call)
  enabled + empty changed/removed -> status=error (no wire call)

[reversible create -> get -> remove] enabled (AXXON_MAP_APPROVE=1) + CONFIRM-map-providers
  input id (lowercase): 50042ffc-bc9d-474c-86c9-7d0cf40c759f
  configure_map_providers(changed=[{id, name:'mcp-probe-provider'}]) -> status=applied
  get_map_provider(id) -> status=ok
    provider id=50042FFC-BC9D-474C-86C9-7D0CF40C759F name=mcp-probe-provider
  configure_map_providers(removed=[id]) -> status=applied
  get_map_provider(id) after remove -> NOT_FOUND (confirmed removed)

[detail] the server stores provider ids UPPERCASE and GetMapProvider/remove are
  case-sensitive; the tool normalizes ids to uppercase so a lowercase GUID input
  round-trips reliably (create -> get -> remove all resolve).

[wire shapes] (probed before building)
  MapService.ConfigureMapProviders({changed:[MapProvider], removed:[id]}) -> {etags}.
    MapProvider is in MapProvider_pb2; api_key/copyright are StringValue (wrapped).
  MapService.GetMapProvider({id}) -> {MapProvider}.

[hygiene]
  Reversible: throwaway provider created then removed; stock providers untouched.
  Writes default-off (env + token). No proto/CA/PDF committed; secrets env-only.

[milestone] MapService is now 11/11 complete.
