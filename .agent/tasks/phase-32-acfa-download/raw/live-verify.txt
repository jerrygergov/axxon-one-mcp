Phase 32 - AcfaService.DownloadData (+ ListUnitsVisualizations read helper)
Live evidence against the demo stand (host <demo-host>, creds <redacted>, ACFA emulator).
Exercised end-to-end THROUGH the shipped MCP tools
(axxon_mcp_acfa_vmda_control.list_unit_visualizations / download_unit_data).
Read-only: downloads existing icon image files, no mutation.

unit: hosts/Server/ACFA.2/EMULATOR_LOCK.5 (virtual lock on the ACFA emulator)

[A] list_unit_visualizations (read)
  status=ok
  visualization ICONS_EMULATOR_LOCK_0 kind=icons ->
    image_data_ids=['ax_percos20v2_r_lock.png', 'ax_percos20v2_r_unlock.png']

[B] download_unit_data (read, metadata only) via AcfaService.DownloadData
  status=ok count=2 total_bytes=688
    {data_id: ax_percos20v2_r_lock.png,   byte_count: 344}
    {data_id: ax_percos20v2_r_unlock.png, byte_count: 344}
  data blob present in response: False (and in items: False)
  (the raw image payload is measured, not echoed; the tool returns size metadata only)

[C] error matrix (no wire call)
  list_unit_visualizations(uids=[])      -> status=error
  download_unit_data(uid="")             -> status=error
  download_unit_data(data_ids=[])        -> status=error

[wire shape] (probed before building)
  AcfaService.ListUnitsVisualizations(items=[{uid}]) -> stream of per-unit
    visualizations; icon visualizations carry an `image` data_id.
  AcfaService.DownloadData(uid, data_id[]) -> stream DownloadDataResponse{data, data_id}.
    data is a base64-encoded PNG (probe head 'iVBORw0K'); each lock icon was 344 bytes.

[hygiene]
  Read-only download of existing icon images; nothing created/changed/deleted on the
  stand. Metadata-only tool surface (no raw media bytes returned). No proto/CA/PDF
  committed; secrets env-only.

[milestone] AcfaService is now 7/7 complete (all RPCs tested-pass).
