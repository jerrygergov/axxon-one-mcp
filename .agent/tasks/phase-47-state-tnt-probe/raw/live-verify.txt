Phase 47 probe-and-classify — StateControlService + TagAndTrackService + ConfigurationManager
Stand: <demo-host> (gRPC TLS :20109, CN=Server). Credentials from env, redacted.
Goal: determine which of the 5 'closeable' pending methods are code-closeable vs stand-walled.

=== Endpoint discovery (DomainService.ListComponents, 500 APs) ===
TelemetryControl: hosts/Server/DeviceIpint.54/TelemetryControl.{0,1,2}
StateControl relays: hosts/Server/DeviceIpint.54/StateControl.relay0:{0,1}
(DeviceIpint.54 is the only device exposing relays/telemetry; added with a generic driver.)

=== StateControlService.SetState (relay, reversible attempt) ===
GetCurrentState relay0:0 -> INTERNAL "Can't resolve reference to .../StateControl.relay0:0"
GetDefaultState relay0:0 -> INTERNAL (same)
GetCurrentState/GetDefaultState relay0:1 -> INTERNAL (same)
SetState ON relay0:0    -> INTERNAL "Can't resolve reference"
=> The relay I/O object is not instantiated by the generic device driver; even the read
   companions (already tested-warn-fixture-needed) fail identically. SetState is stand-walled.

=== TagAndTrackService.SetMode / FollowTrack / MoveToCoords ===
ListTrackers hosts/Server/DeviceIpint.54                         -> NOT_FOUND
ListTrackers hosts/Server/DeviceIpint.54/TelemetryControl.{0,1,2} -> NOT_FOUND
ListTrackers hosts/Server/DeviceIpint.54/SourceEndpoint.video:0:0 -> NOT_FOUND
=> No calibrated Tag&Track tracker object is configured on this stand (ListTrackers, the read
   sibling, is already tested-warn-fixture-needed). SetMode/FollowTrack/MoveToCoords all need
   that tracker; they are stand-walled.

=== ConfigurationManager.SetRevision (NOT probed-as-mutation) ===
GetRevisionInfo SHARED -> 1 node, 461 revisions (read works).
SetRevision rolls the entire shared/local config back to a prior revision -- a whole-system
config restore (the 'restore' half of backup/restore). It is genuinely callable but destructive
and not safely reversible on a shared stand, so it is left pending pending explicit authorization,
alongside its sibling RestoreBackup (also pending).

Conclusion: 4 of the 5 are stand-walled (same class as their already-fixture-warn read siblings)
-> move SetState + TagAndTrack x3 from pending to tested-warn-fixture-needed. SetRevision stays
pending (destructive-only). No stand state was changed (every SetState attempt was rejected
before applying).
