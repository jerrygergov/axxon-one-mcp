=== phase-39-security-credentials live verify (sanitized) ===
date: 2026-06-07T15:26:13Z
host: <demo-host>  grpc: 20109  user: <redacted>  tls_cn: Server
note: all probe passwords/logins are generated throwaway values, redacted below
note: change_my_password and change_my_login are each verified on a SEPARATE throwaway
      admin user, because the module re-authenticates per call (so a password change
      mid-session would invalidate a following call's stored credentials).

connect: {'connected': True, 'enabled': True, 'approval_env': 'AXXON_SECURITY_CREDENTIALS_APPROVE'}
check_password[current pw] -> NOT_UNIQUE
check_password[unused pw ] -> OK
check_password[empty user] -> error
change_my_password (throwaway user A) -> OK
  cleanup A still present= False
change_my_login (throwaway user B) -> OK
  cleanup B still present= False
gate (env off) -> disabled
gate (bad token) -> gap
ALL LIVE CHECKS PASS
