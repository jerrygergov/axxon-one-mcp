# Generated: $TITLE

Template: `$TEMPLATE`

This bundle was generated by `axxon_mcp_generator.py` from the verified MCP
corpus.

## Run

```bash
npm ci
npm run build
npm test
npm start
```

Required environment variables are listed in `.env.example`. Never embed credentials.

## Safety

Credentials are read only from the environment; copy `.env.example` and fill it in locally,
never commit real values. All calls are bounded by duration, count, or byte caps. Mutating
templates refuse to generate unless mutation was explicitly authorised.
